import { TailNumberPipe } from './tail-number-pipe';

describe('TailNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new TailNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
